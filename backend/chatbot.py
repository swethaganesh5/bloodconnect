import os

CHROMA_PATH = "chroma_db"

def load_documents(pdf_path: str):
    return 0

def ask_question(question: str):
    return {
        "answer": "AI chatbot is available in the local version. The deployed version uses lightweight mode to save resources. Please run locally for full AI features.",
        "sources": []
    }